apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-api123
  namespace: default
  labels:
    app: user-api123
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-api123
  template:
    metadata:
      labels:
        app: user-api123
    spec:
      containers:
      - name: user-api123
        image: asia-south1-docker.pkg.dev/dev-zephyr-352206/user-api123/user-api123:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3000"
        - name: OPENWEATHER_API_KEY
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: openweather-api-key
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: user-api123-service
  namespace: default
spec:
  selector:
    app: user-api123
  ports:
  - port: 80
    targetPort: 3000
  type: LoadBalancer
